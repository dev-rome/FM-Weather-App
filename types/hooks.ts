export type DayInfo = {
  date: string;
  dayName: string;
  shortDayName: string;
};

export type FormattedHourlyData = {
  time: string;
  icon: string;
  iconAlt: string;
  temperature: string;
};
